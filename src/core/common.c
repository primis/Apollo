/*
 * (c) 2016 Apollo Developers
 * common.c - Stuff that doesn't quite fit anywhere else
 */

#include <sys/init.h>

// Makefile includes Git build number as a macro string.
const char* gitRevision = GITREV;
